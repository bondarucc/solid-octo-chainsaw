{
  "type": "module",
  "workspaces": [
    "apps/api",
    "apps/ui"
  ],
  "scripts": {
    "build": "npm run build -w=api && npm run build -w=ui",
    "updateAndStart": "git pull && npm install && npm run start",
    "start": "npm run migrate:prod -w=api && npm run start:prod -w=api"
  }
}
